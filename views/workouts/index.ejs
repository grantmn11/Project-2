<%- include('../partials/header') -%>
<main>
  <h1 id='head'>Workouts</h1>
  <form id='new-form'>
    <table>
      <thead>
        <th>User</th>
        <th>Workout</th>
        <th>Intensity</th>
        <th>Date</th>
        <th>Delete</th>
        <th>Details</th>



      </thead>
      <tbody>
        <% workouts.forEach(function(workout){ %>
        <tr>

  </form>
  <td><%= user.name %></td>
  <td><%= workout.workout %></td>
  <td><%= workout.intensity %></td>
  <td><%= workout.date.toLocaleDateString() %></td>

  <td>
    <form class='delete' action="/workouts/<%= workout._id %>?_method=DELETE" method='POST'>
      <button class="btn" type='submit'>X</button>
  </td>
  <td><a class='detail' href="/workouts/<%= workout._id %>">
    <input type="button" class="btn" value="Workout Details">
  </a>
</td>
  </tr>

  <% }); %>
  </tbody>
  </table>
  <ul>
    <li>
      <a href="/">
      <input type='button' class="btn" value="Home">
    </a>
    </li>
    <li>
      <a href="/workouts/new">
         <input type="button" class="btn" value="Add New Workout"> 
        </a>
    </li>
    
  </ul>
  </form>
</main>

<%- include('../partials/footer') -%>